# 📋 Журнал передачи оборудования и обходов

Telegram-бот для автоматического ведения журнала обходов охраны с голосовым/текстовым вводом и веб-интерфейсом.

[![Live Demo](https://img.shields.io/badge/demo-live-success)](https://gagarinyury.github.io/voice-work-telega/)
[![Cloudflare Workers](https://img.shields.io/badge/Cloudflare-Workers-orange)](https://workers.cloudflare.com/)

## 🎯 Возможности

- ✅ **Голосовой и текстовый ввод** через Telegram
- 🤖 **Автоматическое распознавание** через Gemini AI
- 📊 **Веб-таблица** с историей записей
- 🔐 **Rate limiting** (10 запросов/час на пользователя)
- 👥 **Ограничение** на 4 пользователя
- ✅ **Верификация через Telegram**
- 📝 **Учет оборудования** (пульты, планшет, ключи, телефон, кнопка TS)

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram Bot   │  ← Пользователи отправляют голос/текст
│   @olivulabot   │
└────────┬────────┘
         │
         ↓ Webhook
┌─────────────────────────────────────┐
│   Cloudflare Worker (Backend)      │
│  ┌──────────────────────────────┐  │
│  │ 1. Прием сообщений           │  │
│  │ 2. Распознавание через Gemini│  │
│  │ 3. Парсинг в JSON            │  │
│  │ 4. Сохранение в D1           │  │
│  │ 5. API для фронтенда         │  │
│  └──────────────────────────────┘  │
└────────┬────────────────┬───────────┘
         │                │
         ↓                ↓
┌────────────────┐  ┌──────────────┐
│  D1 Database   │  │ Gemini 2.0   │
│   (SQLite)     │  │  Flash API   │
└────────────────┘  └──────────────┘
         ↓
┌─────────────────────────────────┐
│  GitHub Pages (Frontend)        │
│  - Таблица с записями           │
│  - Автообновление каждые 30 сек │
└─────────────────────────────────┘
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm
- Cloudflare аккаунт
- Telegram Bot Token
- Google Gemini API Key

### 1. Клонирование репозитория

```bash
git clone https://github.com/gagarinyury/voice-work-telega.git
cd voice-work-telega
```

### 2. Установка Wrangler CLI

```bash
npm install -g wrangler
wrangler login
```

### 3. Создание D1 базы данных

```bash
wrangler d1 create equipment_journal
```

Скопируй `database_id` и вставь в `wrangler.toml`:

```toml
[[d1_databases]]
binding = "DB"
database_name = "equipment_journal"
database_id = "ВАШ_DATABASE_ID"
```

### 4. Создание таблиц

```bash
wrangler d1 execute equipment_journal --file=schema.sql --remote
```

### 5. Настройка секретов

Добавь секреты в Cloudflare Dashboard:

1. Открой https://dash.cloudflare.com
2. Workers & Pages → equipment-journal-worker
3. Settings → Variables and Secrets
4. Добавь переменные (с типом **Encrypt**):
   - `TELEGRAM_BOT_TOKEN` - токен от @BotFather
   - `GEMINI_API_KEY` - ключ с https://aistudio.google.com/apikey

### 6. Деплой Worker

```bash
wrangler deploy
```

### 7. Настройка Telegram Webhook

```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://equipment-journal-worker.<ВАШ_ПОДДОМЕН>.workers.dev/webhook"
```

### 8. Настройка GitHub Pages

1. Зайди в Settings репозитория
2. Pages → Source: **Deploy from a branch**
3. Branch: **main**, folder: **/ (root)**
4. Save

Сайт будет доступен: `https://<ваш_username>.github.io/voice-work-telega/`

## 📱 Использование бота

### Регистрация

1. Найди бота в Telegram: `@olivulabot`
2. Отправь `/start`
3. Введи свою фамилию

### Отправка записи

**Голосом:**
```
"Обходы девять десять, двенадцать пятнадцать.
Садовники приехали семь ноль пять, уехали пятнадцать сорок.
Закрытие роллетов двадцать ноль пять"
```

**Текстом:**
```
Обходы 09:10, 12:15. Садовники приехали 07:05, уехали 15:40. Закрытие роллетов 20:05
```

### Ответ бота

```
✅ Запись добавлена:

👤 Иванов
📅 09.12.2025

🚶 Обходы:
09:10, 12:15

📝 События:
  • 07:05 - Садовники приехали
  • 15:40 - Садовники уехали
  • 20:05 - Закрытие роллетов
```

## 🗂️ Структура проекта

```
equipment-journal/
├── worker.js           # Cloudflare Worker (backend)
├── index.html          # Веб-интерфейс (frontend)
├── schema.sql          # Схема базы данных
├── wrangler.toml       # Конфигурация Worker
├── package.json        # npm пакеты
├── DEPLOY.md          # Детальная инструкция по деплою
└── README.md          # Этот файл
```

## 🛢️ База данных

### Таблица `users`
```sql
- telegram_id (INTEGER PRIMARY KEY)
- surname (TEXT)
- created_at (DATETIME)
```

### Таблица `journal`
```sql
- id (INTEGER PRIMARY KEY)
- telegram_id (INTEGER)
- surname (TEXT)
- date (DATE)
- items (TEXT JSON)  -- Оборудование
- rounds (TEXT JSON) -- Время обходов
- events (TEXT JSON) -- События
- created_at (DATETIME)
```

### Таблица `rate_limits`
```sql
- telegram_id (INTEGER PRIMARY KEY)
- request_count (INTEGER)
- window_start (DATETIME)
```

## 🔒 Безопасность

- ✅ API ключи хранятся в **Encrypted Variables** Cloudflare
- ✅ **Rate limiting**: 10 запросов/час на пользователя
- ✅ **Лимит пользователей**: максимум 4
- ✅ HTTPS для всех запросов
- ✅ CORS настроен только для GitHub Pages

## 🎨 Формат данных

### Gemini API Response Schema

```json
{
  "rounds": ["09:10", "12:15", "16:30"],
  "events": [
    { "time": "07:05", "description": "Садовники приехали" },
    { "time": "15:40", "description": "Садовники уехали" }
  ]
}
```

### Оборудование (items)

```json
{
  "pults": true,
  "tablet": true,
  "keys": true,
  "phone": true,
  "ts_button": true
}
```

## 📊 API Endpoints

### `GET /api/journal`
Возвращает последние 100 записей журнала.

**Response:**
```json
[
  {
    "surname": "Иванов",
    "date": "2025-12-09",
    "items": "{\"pults\":true,...}",
    "rounds": ["09:10", "12:15"],
    "events": [
      {"time": "07:05", "description": "Садовники приехали"}
    ],
    "created_at": "2025-12-09T10:30:00Z"
  }
]
```

### `POST /webhook`
Webhook для Telegram Bot API (только для Telegram серверов).

## 🔧 Конфигурация

### Rate Limiting

В `worker.js`:
```javascript
const MAX_REQUESTS = 10;      // Запросов в окно
const WINDOW_MINUTES = 60;    // Размер окна в минутах
```

### Лимит пользователей

В `worker.js`:
```javascript
if (usersCount.count >= 4) {  // Максимум 4 пользователя
  // Блокировка регистрации
}
```

## 🧪 Тестирование

### Локальная разработка

```bash
wrangler dev
```

Worker будет доступен на `http://localhost:8787`

### Проверка логов

```bash
wrangler tail
```

### Тест API

```bash
curl https://equipment-journal-worker.yurcheg13.workers.dev/api/journal
```

## 📈 Лимиты и квоты

### Cloudflare Workers (Free Tier)
- 100,000 запросов/день
- 10ms CPU time на запрос
- Неограниченная пропускная способность

### Gemini API (Free Tier)
- 1,500 запросов/день (Gemini 2.0 Flash)
- 1 млн токенов/месяц

### GitHub Pages
- 100 GB/месяц пропускной способности
- 100 сборок/час

## 🐛 Troubleshooting

### Бот не отвечает

1. Проверь webhook:
```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
```

2. Проверь логи:
```bash
wrangler tail
```

### Ошибка Gemini API

- Проверь квоту на https://aistudio.google.com
- Убедись что ключ правильный в Cloudflare Dashboard

### База данных не работает

```bash
# Проверь таблицы
wrangler d1 execute equipment_journal --remote --command "SELECT * FROM users"
```

## 🚀 Roadmap

- [ ] Накопительный режим (несколько сообщений → одна запись)
- [ ] Команды редактирования (`/edit`, `/delete`)
- [ ] Расписание дежурств (привязка дней к пользователям)
- [ ] Экспорт в Excel/PDF
- [ ] Статистика по пользователям
- [ ] Push-уведомления о пропущенных обходах

## 📝 Лицензия

MIT

## 👤 Автор

Создано с помощью [Claude Code](https://claude.com/claude-code)

---

**Live Demo:** https://gagarinyury.github.io/voice-work-telega/

**Telegram Bot:** @olivulabot
